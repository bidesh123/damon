
Then /^I click on the edit button for requisition "([^\"]*)"$/ do |req_id|
  req = RequisitionHeader.find req_id.to_i
  with_scope("tr#requisition_header_row_#{req_id}") do
    with_scope("a[title=Edit]") do

    end
  end
end
